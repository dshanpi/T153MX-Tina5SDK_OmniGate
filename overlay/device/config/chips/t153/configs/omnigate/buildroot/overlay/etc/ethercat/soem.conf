# Dedicated EtherCAT port. Keep this interface out of DHCP/bridge setup.
SOEM_INTERFACE=eth0

# Optional real-time settings used by soem-run. Values can be overridden
# per invocation or edited here after deployment.
SOEM_PRIORITY=80
SOEM_CPU=2
