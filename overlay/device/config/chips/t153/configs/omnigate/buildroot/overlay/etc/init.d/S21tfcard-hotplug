#!/bin/sh

READY_FILE="/run/tfcard-hotplug-ready"

case "$1" in
	start)
		mkdir -p /run
		: > "${READY_FILE}"
		;;
	stop)
		rm -f "${READY_FILE}"
		;;
	restart)
		"$0" stop
		"$0" start
		;;
	*)
		echo "Usage: $0 {start|stop|restart}"
		exit 1
		;;
esac
